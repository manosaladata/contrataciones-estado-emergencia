---
title: "Untitled"
author: "Abner Francisco Casallo Trauco"
date: "1/10/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(DT)
df <- head(iris)
df %>% 
  datatable %>% 
  formatStyle(1:4, color = JS("value % 1 === 0 ? 'red' : ''"))

m <- matrix(F, ncol = ncol(df)-1, nrow = nrow(df))
m[rbind(c(2,2),c(5,1))] <- TRUE
df %>% 
  cbind(m) %>% 
  datatable(
    options=list(columnDefs = list(list(visible=FALSE, targets=1:4+ncol(df)))),
  ) %>% 
  formatStyle(
    columns = 1:4, 
    valueColumns = 1:4+ncol(df), 
    color = styleEqual(c(1,0), c("red", "black"))
  )

df

m <- "intialize" # initialize, sometimes better as just `list()`
for(i in 1:nrow(g)){
  if(g[i,2]=='No'){
    # paste into position i of vector m
    m[i] <- paste0("ABC", g[i,1], "DEF", g[i,2], "GHI", g[i,3],"\n") 
  } else if(g[i,2]=='Yes'){
    # paste into position i of vector m
    m[i] <- paste0("ABC", g[i,1], "DEF", g[i,2], "GHI", g[i,3],"\n")
  } else {
  NULL
  }
}
m
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
#summary(cars)

library(DT)
library(datasets)
data(iris)
#head(iris)
#names(iris)

#for(i in iris[,1]){if (i>5) { 
  b<-datatable(iris) %>%
      formatStyle(columns="Sepal.Length", 
                  #color = 'red', 
                  background = styleInterval(c(4.9), c("white", "lightblue")),
                  color = styleInterval(c(4.9),c("white", 'orange')))
  #Pinta por intervalos, acá se divide en dos grupos(menores o igual a 4.9 y mayores a 4.9), se podría dividir en 3 o más...
  
                  #,background = styleEqual(c(4.7, 5), c("magenta", "lightblue"))) n        #Pinta valores específicos
                    #}}

b

```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
